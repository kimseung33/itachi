<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="itachi.uchiha.mapper.board">
	<select id="login" resultType="MemberDTO">
		select * from itachi_mb
		where
		mb_id=#{mb_Id} and mb_pw=#{mb_Pw}
	</select>
	
	<select id="selectid" resultType="MemberDTO">
		select mb_id from itachi_mb where mb_hp=#{mb_hp} and mb_birth=#{mb_birth}
	</select>
	
	<select id="selectpw" resultType="MemberDTO">
		select mb_id from itachi_mb where mb_id=#{mb_id} and mb_birth=#{mb_birth}
	
</select>

	<select id="updateui" resultType="MemberDTO">
		select * from itachi_mb where mb_id=#{mb_id}
	</select>
	


	<update id="update">
		update itachi_mb set
		mb_pw=#{mb_pw},
		mb_zip=#{mb_zip},
		mb_addr1=#{mb_addr1},
		mb_addr2=#{mb_addr2},
		mb_addr3=#{mb_addr3},
		mb_email=#{mb_email}
		where mb_id=#{mb_id}
	</update>

</mapper>

<!-- <mapper namespace="itachi.uchiha.mapper.board">
	<select id="login" resultType="MemberDTO">
		select * from itachi_mb
		where
		mb_id=#{mb_Id} and mb_pw=#{mb_Pw}
>>>>>>> branch 'master' of https://github.com/kimseung33/itachi.git
	</select>
</mapper> -->
